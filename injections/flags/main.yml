patterns:
  - include: "#main"
scopeName: injection.flag.o3
injectionSelector: L:meta.tag
repository:
  main:
    patterns:
      - begin: (\-{2})(for)(\=\")
        end: (\")
        beginCaptures:
          2:
            name: markup.italic support.function
        name: source.ts
        patterns:
          - match: (as)\s+(\()(.+)(\))
            name: variables
            captures:
              1:
                name: constant.type
              3:
                name: source.ts
                patterns:
                  - include: source.ts
          - include: punctuation
          - include: source.ts
      - begin: (\-{2}|\:)([^\s\t]+)(\=\")
        end: (\")
        beginCaptures:
          2:
            name: markup.italic support.function
        name: source.ts
        patterns:
          - include: punctuation
          - include: source.ts
      - match: ((\-{2})([^\s\t\=\>\<]+)+(?!\=))
        name: markup.italic support.function
